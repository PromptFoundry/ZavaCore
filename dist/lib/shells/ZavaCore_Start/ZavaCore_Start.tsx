import Layout from '../../../components/Layout';

/**
 * ZavaCore_Start - Full ZavaCore application template
 *
 * Pre-configured template with the ZavaCore left nav, header,
 * hero card, agent chat interface, and widget sections.
 *
 * @example
 * ```tsx
 * import { ZavaCore_Start } from '@/lib';
 *
 * export default function App() {
 *   return <ZavaCore_Start />;
 * }
 * ```
 */
export default function ZavaCore_Start() {
  return <Layout />;
}
